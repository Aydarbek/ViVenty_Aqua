sqlcmd -H127.0.0.1 -SA80042201HP -Usa -PFhpfvfc-16

SELECT name FROM master.sys.databases;
USE Aqua_DB;

SELECT * FROM Aqua_DB.INFORMATION_SCHEMA.TABLES;
SELECT Id, Login, Password, Address,  Role FROM Users;

SELECT TOP 5 PhotoId, Path FROM Photos;
DELETE FROM Photos WHERE PhotoId = 4;



INSERT INTO Categories (Name, Description)
VALUES	('Swimming Suites', 'Cool and modern models of suits for swimming'), 
		('Sport Suites', 'Cool and modern models of suits for aqua sports - Kiting, Waking and Surfing');


INSERT INTO Categories (Name, Description)
VALUES	('Водные костюмы', 'Яркие и современные водные костюмы для водных видов спорта');

INSERT INTO Categories (Name, Description)
VALUES	('Купальники', 'Красивые яркие купальники современных модных трендов для активного и пляжного отдыха');


INSERT INTO Hsuits (Name, Description, Price, DefaultPhoto, Category_Id)
VALUES		('Марин', 'Хорошая модель для кайтинга', 7000, 'SpSuit_M1_front.jpg', 1),
		('Касатка', 'Хорошая модель для серфинга', 8000, 'SpSuit_M2_front.jpg', 1),
		('Ариэль', 'Идеальная модель для серфинга', 7500, 'SpSuit_M3_front.jpg', 1),
		('Изабелла', 'Классный современный купальник', 6000, 'SwSuit_S1_front.jpg', 2),
		('Волна', 'Изящный и яркий купальный костюм', 5000, 'SwSuit_S2_front.jpg', 2);


UPDATE Photos 
SET hsuit_id = 2 WHERE PhotoId = 3 OR PhotoId = 5;





CREATE TABLE Categories (
Id INT NOT NULL IDENTITY(1,1) PRIMARY KEY,
Name NVARCHAR(16) NOT NULL,
Description NVARCHAR(128) NOT NULL
);



CREATE TABLE Hsuits (
Id INT NOT NULL IDENTITY(1,1) PRIMARY KEY,
Name VARCHAR(32) NOT NULL,
Category INT NOT NULL,
Description VARCHAR (256) NOT NULL,
Size VARCHAR (10),
Price INT NOT NULL,
Default_photo VARCHAR (128),
FOREIGN KEY (Category) REFERENCES Categories (Id));